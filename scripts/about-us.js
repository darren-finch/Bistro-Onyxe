const lines = document.getElementsByClassName("line");
for (var line of lines) {
	console.dir("Line: " + line);
	//   const width = line.offsetWidth;
	//   const height = line.offsetHeight;
	//   line.style.transform = "rotate(" + Math.acos(height / width) + "rad)";

	//   const contentAreaWidth = document.getElementsByClassName("content")[0]
	//     .offsetWidth;
	//   line.style.left = "150px";
	//   line.style.right = contentAreaWidth - 150 + "px";
}
